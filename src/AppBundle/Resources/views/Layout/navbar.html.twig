<nav class="navbar navbar-inverse navbar-fixed-top">
    <div class="container-fluid">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse"
                    data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="{{ path('app_admin_dashboard') }}">Administration</a>
            <a class="navbar-brand" href="{{ path('publication_index') }}">Publication</a>
            <a class="navbar-brand" href="{{ path('science_index') }}">Science</a>
            <a class="navbar-brand" href="{{ path('fos_user_profile_show') }}">Profile</a>
        </div>

        <div id="navbar" class="navbar-collapse collapse">
            <ul class="nav navbar-nav navbar-right">
                <li><a href="{{ path('app_home') }}">Partie publique</a></li>
                <li><a class="navbar-brand" href="{{ path('fos_user_registration_register') }}">Register</a></li>

                {#{% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}#}
                    {#<a class ="textnav" href="{{ path('fos_user_security_logout')}}"><span class="glyphicon glyphicon-log-out"></span class ="textnav"> {{ 'navbar.log.logout'|trans }}</a>#}
                {#{% else %}#}
                    {#<a class ="textnav" href="{{ path('fos_user_security_login')}}"><span class="glyphicon glyphicon-log-in"></span> {{ 'navbar.log.login'|trans }}</a>#}
                {#{% endif %}#}
                {##}



                <li>{% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                    {{ 'layout.logged_in_as'|trans({'%username%': app.user.username}, 'FOSUserBundle') }} |
                    <a class="navbar-brand" href="{{ path('fos_user_security_logout') }}"><span class="glyphicon glyphicon-log-out"></span>
                        {{ 'layout.logout'|trans({}, 'FOSUserBundle') }}
                    </a>
                {% else %}
                    <a class="navbar-brand" href="{{ path('fos_user_security_login') }}"><span class="glyphicon glyphicon-log-in"></span>{{ 'layout.login'|trans({}, 'FOSUserBundle') }}</a>
                    {% endif %}</li>

                {#<li><a class="navbar-brand" href="{{ path('fos_user_security_login') }}">Login</a></li>#}
            </ul>
        </div>
    </div>
</nav>
