{% extends '::base.html.twig' %}

{% block content %}
            <h1>Ajouter un nouveau animateur</h1>
            {# Début formulaire (ajout ou modification) #}
			    {{ form_start(form) }}
                {# Champ du formulaire #}
			   {{ form(form) }}
            {# Fin formulaire #}
            {{ form_end(form) }}
            <p>
            <a href="{{ path('app_animateur_liste') }}" class="btn btn-sm btn-danger">Annuler</a>
            </p>

{% endblock %}