{% extends '::base.html.twig' %}

{% block content %}
 <div class="jumbotron">
        <div class="container">
            <h1>Ajouter un nouveau cours</h1>

            {# Début formulaire (ajout ou modification) #}
			    {{ form_start(form) }}
                {# Champ du formulaire #}
			   {{ form(form) }}
            {# Fin formulaire #}
            {{ form_end(form) }}
        </div>
    </div>

{% endblock %}